const view = {
    addEvents: () => {
        const cashDOM = document,
              columnTitle = cashDOM.getElementById('title');
              columnData = cashDOM.getElementById('data');
              addButton = cashDOM.getElementById('send-data');
    },
};

export default view;